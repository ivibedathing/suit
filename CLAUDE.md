# CLAUDE.md

All guidance for working in this repository lives in **`AGENTS.md`** — go read it before doing
anything else. It has the build/test commands, the architecture and file map, and the
load-bearing workflow rules (worktree-first, no SwiftPM/Xcode, document shipped features).
